go 1.26.5

// TODO: remove this go.work file once packages/core/vX.Y.Z is first tagged
// and published (see .github/workflows/job-publish-app-core-packages.yml,
// gated behind vars.CORE_PACKAGE_RELEASED). After that, replace it with a
// normal pinned dependency in go.mod:
//   go get github.com/litelensapp/litelens/packages/core@packages/core/vX.Y.Z
use (
	.
	./packages/core
)
